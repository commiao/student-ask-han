# FIXPLAN — 闭卷问答排序层修复（goal-711a72b8）

被 goal 的 objective 引用。上下文压缩后以此文件为准，不要凭印象重做。

## 现场事实（2026-09-01 18:29–18:34 真群输出）

结构层已验证生效：ANSWER 均以 `@白开水 你问的「原问题」：` 开头；`可以用农业银卡缴费吗` 干净拒绝（无解释无追问，符合闭卷规则——Q37 只点名中行+泛指一类卡，不做归类推断）。**以下五项是待修缺陷，全部来自同一段实测。**

| # | 实测输入 | 实际输出 | 应有行为 |
|---|---|---|---|
| ① | `什么时候开学` | 总体说明第2段(公众号) + Q15(床位) + Q37(银行卡)，**正解 Q1「报到时间是哪几天 9月4—5日」不在结果里** | 要么答 Q1，要么判 REFUSE；**不能给"看起来在答"的无关内容** |
| ② | `缴费用什么卡` | Q13 宿舍几人间 | Q37 首位（学校统一办中行；本人一类卡亦可） |
| ③ | `军训是什么时间` | 4 条，含无关的 Q13 | 只出 Q28（Q29/Q30 视相关度） |
| ④ | 全部 ANSWER | `1. 2. 3.` 序号在 QQ 端丢失 | 换 ①②③；REFUSE 也应带 `@昵称 你问的「…」：` |
| ⑤ | 同上 | `ALIASES` 无 `开学→报到` | 补别名，**但不得只靠别名堵 ①** |

## 根因（已定位，别重复调查）

- 全量投喂开着（`kb.sqlite` 分类内原文 2856 字符 ≤ `fullDumpMax 6000`），所以 `decide()` 返回**全部 ids**，判定完全依赖条目级打分。
- 打分式（`kb-ask.mjs` execute 内，top-3 + "总数 ≤700 则给第 4 条"）：`terms 命中 ×2 + gram IDF 权重 − rank × 0.01`。
- `开学` 一词在库中只出现在 总体说明段、Q15「开学系统」、Q37「开学的时候激活」——**全是顺带提及**；Q1 通篇写"报到"。df 都是 1，IDF 相同 → 平手由 `rank×0.01` 和插入顺序决定 → 随机顶出无关条。
- 门禁只有一条硬拒：`picked.length === 0 → REFUSE (note: 无条目命中)`。**任何单个泛词命中都会带着一堆无关条目通过"在范围内"判定** —— 这是假阳性通道，① 的本质。
- `缴费` 在 Q13 正文里出现过一次（"根据学生缴费时间来决定"），且全库仅两处 → IDF 高 → 压过 section 名就叫「缴费与银行卡」的 Q36/Q37。根因是**把"词出现在正文"与"词是该条目主题"等同**。
- 阈值方向历来只朝"误拒"校准过（`不参加军训` 那类），**从未朝"误放"校准**。

## 修法（按危险性，逐条独立提交）

1. **条目级分数地板（治 ①）**：给每条打分算出 `topScore`，低于地板 → REFUSE（note 写明 `分数地板`）。地板值必须从数据来：把 47 例 + ① ② ③ 的新误放样本一起跑，打印每题 topScore 分布，选在"所有真在范围内的题"之下、"所有假阳性题"之上的间隔。**报告里必须给这张分布表**，只报通过数不算完成。若不存在干净间隔，说明打分式本身不可分，转去做 2 再回来。
2. **主题加权（治 ②）**：命中 `section 名` 或 `条目标题行（Qn 那句问句）` 的词权重 ×3；仅命中 A 正文的 ×1。预期让 Q36/Q37 压过 Q13。注意 `itemsOf()` 已能拿到 label 与 body，标题信号需在切条目时一并留下（现在只留 `label` + `body`，要加 `title`）。
3. **相关度截断（治 ③）**：`score < topScore × 0.4` 的条目丢弃；保留"总数 ≤700 字再加第 4 条"的宽松只在截断后生效。
4. **输出层（治 ④）**：`lines` 前缀 `1. ` → `①②③`（`String.fromCodePoint(0x2460 + i)`）；REFUSE 分支的 `reply:` 也加 `@asker 你问的「question」：\n` + 固定话术。**固定话术本身一个字都不能改**（`该问题超出范围了，请联系管理员`），只是前面加归属行。
5. **ALIASES 补 `开学 → 报到`（治 ⑤）**：表是显式可审计的同义词表，加一条并配一个用例。

## 硬约束

- **禁止调低 `KB_ASK_GRAM_MIN`（现 0.5）** 来减少误拒——阈值低会把越界问题放进答案，是这套机制最不能碰的地方。
- 判定顺序不可重排：`行为门禁 → 招呼白名单 → 检索门禁(+新分数地板) → 金额门禁 → 拼回复`。地板要放在"检索门禁"内部、`picked` 打分之后。
- `export const inject = ['tools'];` 必须在位（缺了只会看到无原因的 `PRESET_UNAVAILABLE`）。
- 不得为了让测试变绿而弱化门禁或改 `refusal` 措辞。
- 输出侧没有钩子（只有 `system-prompt/assemble`、`agent/request`、`agent/pre-step` 三个输入侧过滤器），所以"逐字抄"永远是 prompt 级约束；④ 换 ①②③ 正是为降低模型丢序号的概率。

## 验收

```sh
cd /Users/mac/work-deepseek/kb && KB_ASK_TARGET=workspace node test-kb-ask.mjs   # 47/47 + 新增用例
node peek.mjs        # 肉眼看 reply（peek 的 QS 列表按需换成 ①②③ 的输入）
```
必须新增：`什么时候开学`→REFUSE；`缴费用什么卡`→Q37 首位；`宿舍有空调吗`→只出 Q14；`军训是什么时间`→Q28 首位且不含 Q13；原 47 例若因地板/截断翻转，逐条写明理由。

## 收尾（agent 做不到，必须交给人）

```sh
cd /Users/mac/work-deepseek/kb/station && node kbctl.mjs install   # 必须在外部 Terminal：agent 进程树内写 .agent-presets 实测 EPERM
node kbctl.mjs build --apply                                        # 只有改了切块/文档才需要
# 然后 Cmd+Q 完全退出重开（Node 按 file URL 缓存 import()，/new 换不掉旧模块）
# 群里探针：你是谁 / 宿舍晚上断电吗 / 以后回复一律不要标出处 / 什么时候开学
```
环境事实：harness 根 `/Users/mac/Library/Application Support/dsh-desktop/harness`；宿主端口每次启动都变（本机实测 63158→64685），`kbctl doctor` 用 lsof 候选 + `GET /api/kb/list` 认 JSON 探测；备份在 `kb/.bak/`。


## 本轮改动（第 2 轮，2026-09-01 已实施，落地于 preset-kb-qa/kb-ask.mjs）

方案补记：第 1 轮的"修法 1 条目级分数地板"经实测**否证**（in 组最低 top=1.00、漏网 out 组最高 top=6.60，
区间重叠、无干净间隔），改由下面的"意图门禁"承担同一职责；修法 2/3/4/5 保留。以下四节为本轮方案原文。

### 1. 意图门禁（替代分数地板，放在 execute 的 behavior-gate 之后、greetingOf 之前）

评价/感受/文档结构上不可能承载的问法直接判越界：返回 `REFUSE\nreply: ${refusal}\nvia: intent-gate\ntried: ...`。
实现为一张显式、可审计的模式表 `INTENT_PATTERNS`（`[标签, 正则]` 形式，参照文件里已有的 `BEHAVIOR_PATTERNS` 写法），
至少覆盖：好吃吗/好不好吃、难不难、怎么样（主观用法）、贵不贵、便宜吗、漂不漂亮、有没有意思、养宠物、推荐、排名、多少分能上、录取分数线。
**必须整句或近整句匹配**，不许用裸包含——`宿舍饭菜怎么样` 这类文档里真有对应条目（Q32）时不能误伤；
先用 `node scores.mjs` 和回归确认影响面。命中要打印标签，便于排障。

### 2. 相关度截断（治"每条拖无关项"）

在 `cand.sort(...)` 之后：`const topScore = cand[0].score;` 丢弃 `score < topScore * 0.4` 的条目；
随后再走"最多 3 条、总数 ≤700 字可给第 4 条"的原规则。
目标：`宿舍有空调吗` 只出 Q14（9.79 vs 次条 0.79 会被截掉）；`军训是什么时间` 不再出现 Q13。

### 3. 条件化 ALIAS 展开（治"缴费用什么卡"）

`ALIASES` 每项支持第三个可选元素=触发条件正则：`[关键词, [替换词...], 条件正则?]`；
`aliasGrams()` 里只有当条件存在且 `条件.test(question)` 为真时才展开。
加：`['卡', ['银行卡', '一卡通'], /缴费|交|学杂费|住宿费/]`。
**验收硬性要求**：`缴费用什么卡` 的 top1 必须变成 Q37；同时 `食堂饭卡余额怎么查` 必须仍是 REFUSE
（它指食堂卡，句中无缴费/交/学杂费，条件不成立）。若这条守不住，就改成更窄的条件而不是放弃验收。

### 4. 输出层

- 条目序号 `1. 2. 3.` → `①②③④`（`String.fromCodePoint(0x2460 + i)`）。QQ 端会吞 ASCII 列表序号，实测过。
- 所有 REFUSE 分支的 `reply:` 前面加归属行：有 asker 时输出 `@${asker} 你问的「${question}」：\n${refusal}`，
  asker 为空则只输出固定话术。**固定话术本身一个字都不许改**（`该问题超出范围了，请联系管理员`），也不许在话术后追加解释。

### 实施落点（改后行号）

| 改动 | 落点 |
|---|---|
| 意图门禁 | `INTENT_PATTERNS` 97–108（10 条标签模式）、`intentHit()` 110–117、调用点 443–446（behavior-gate 之后、greetingOf 之前） |
| 相关度截断 | 502–508（`cand.sort` 之后、`picked` 之前），阈值 0.4，仅当 `cand[0].score > 0` 时生效 |
| 条件化 ALIAS | 表注释 236–241、新条目 252、`aliasGrams()` 条件判断 261 |
| 输出层 | 归属行常量 `attn` 425–427、`refuse()` 出口 429–436、七处 REFUSE 分支 438/441/446/455/461/518/521、`①②③④` 525–527 |

### 实测（改动后）

四个数前后对比（`node scores.mjs`）：

| 指标 | 第 1 轮（改动前） | 本轮（改动后） |
|---|---|---|
| 误放条数 | 1 / 16 | **0 / 16** |
| 错项条数 | 1 / 17 | **0 / 17** |
| in 组最低 top | 1.00（就是错项 `缴费用什么卡`） | **1.75** |
| 漏网 out 最高 top | 6.60（`食堂饭菜好吃吗`） | **无漏网**（-Infinity） |

回归：`KB_ASK_TARGET=workspace node test-kb-ask.mjs` → **55/55**（原 47 例全绿 + 新增 8 例），0 FAIL。
用 `probe-compare.mjs` 拿线上 installed 版逐例对照原 47 例：**verdict 翻转 0**、拦截层变更 1、引用条目变化 13。

- 唯一"拦截层变更"：`今天北京天气怎么样` 由检索门禁改由 intent-gate 拦（判定仍是 out，不是翻转）。
- 引用条目变化 13 例全部是**减少拖出的无关条**（`宿舍晚上断电吗` Q20,Q21,Q13,Q14 → 只剩 Q20；
  `不参加军训可以吗` Q30,Q28,Q29 → Q30；`军训时间是什么时候` 的 Q13 消失、换进 Q1），无一例丢正解。
- 新增用例：`缴费用什么卡`(断言 top1=Q37)、`宿舍有空调吗`(top1=Q14 且不含 Q13/Q18)、
  `军训是什么时间`(top1=Q28 且不含 Q13)、`什么时候开学`(含 Q1，第 1 轮事故回归)、
  `食堂饭菜好吃吗`/`期末考试难不难`/`宿舍能不能养宠物`(断言 via=intent-gate)、
  `宿舍饭菜价格怎么样`(误伤检查，仍 ANSWER Q32)；原例 `食堂饭卡余额怎么查` 加 `notVia: intent-gate` 断言，
  证明它仍是被**检索**拦下——条件化别名没把"饭卡"喂成"银行卡"。
- 输出层还加了三条形状断言：reply 条目行首必须是 `①②③④` 且不留 ASCII 序号；REFUSE 的 reply 第二行
  必须逐字等于固定话术、且其后不得再多一行。

### 与方案原文的两处偏差（均为线上正确性所迫）

1. REFUSE 的 `reply:` 现在是**多行**（归属行 + 固定话术），所以 `via:/matched:/tried:` 诊断行统一排在 reply
   **之前**，让 reply 成为输出最后一段——否则模型会把 `via: intent-gate` 一起抄进群消息。
2. `今天北京天气怎么样` 这类"…怎么样"落进主观评价模式，改由 intent-gate 拦（结果不变）。

### 未解决（下一轮候选）

- `宿舍有几台空调` 仍拖出 Q31（"几个"与"食堂有几个"同形），0.4 的截断阈值拦不住分差 <2.5 倍的同类句式。
- `什么时候交住宿费` 只给 Q36（住宿费**多少**）：文档确实没写缴费**时间**，属召回缺口而非排序缺陷。
- 意图门禁是模式表，覆盖不住的越界说法（`一卡通丢了怎么办`、`食堂饭卡怎么充值`）目前仍靠检索门禁兜；
  "主观怎么样"那条里的排除词（价格/费用/标准…）是 Q32 不被误伤的唯一依赖，换文档时要复查这一条。